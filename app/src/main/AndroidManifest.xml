<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.roy_sun.awesomeshop_imitative"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".base.BaseApplication"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.roy_sun.awesomeshop_imitative.ui.activity.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.activity.LoginActivity" />
        <activity android:name=".ui.activity.RegisterActivity"/>

        <activity android:name=".ui.activity.Home.SaleActivity"/>
        <activity android:name=".ui.activity.Home.TimeActivity"/>
        <activity android:name=".ui.activity.Home.HotActivity"/>
        <activity android:name=".ui.activity.Home.NewActivity"/>
        <activity android:name=".ui.activity.Home.RecommendActivity"/>
        <activity android:name=".ui.activity.Home.ProductInfoActivity"/>
    </application>

</manifest>
